/** Native block type used whenever an editor command needs a writing fallback. */
export const DEFAULT_BLOCK_TYPE = "paragraph";

/** Reserved native property that persists whether a block's children are folded. */
export const BLOCK_COLLAPSED_PROP = "collapsed";

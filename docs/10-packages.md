# Packages

Rivto packages separate canonical editor behavior from browser presentation. Each package has one focused ownership boundary and can be consumed independently where its layer is appropriate.

The nested pages document installation, public entry points, lifecycle ownership, and package-specific validation commands.

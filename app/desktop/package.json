{
  "name": "@chulane/rivto-app-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc -p tsconfig.json && electron .",
    "build": "tsc -p tsconfig.json",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "start": "electron ."
  },
  "devDependencies": {
    "@types/node": "^22.16.5",
    "electron": "^37.2.1",
    "typescript": "5.8.3"
  }
}
